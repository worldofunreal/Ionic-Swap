Todo

frontend:
create stores for all the data that is needed in the frontend
eg. token balances, pools, etc.
so we pull the data from the stores instead of the canister service
and only refresh the data in the background
eg. when the user inputs actions, we refresh the data in the background


so we will also need the neuron dissolving mechanism